# UTrack Frontend Environment Variables

# Backend API URL
REACT_APP_API_URL=http://localhost:5000

# Instructor Signup Token (required for instructor registration)
# This token must be provided by instructors during signup as a security measure
# IMPORTANT: Change this to a secure token in production!
REACT_APP_INSTRUCTOR_TOKEN=pok
